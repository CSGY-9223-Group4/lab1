# To get started with Dependabot version updates, you'll need to specify which
# package ecosystems to update and where the package manifests are located.
# Please see the documentation for all configuration options:
# https://docs.github.com/code-security/dependabot/dependabot-version-updates/configuration-options-for-the-dependabot.yml-file
version: 2
updates:
  - package-ecosystem: "pip"         # Specifies the package ecosystem
    directory: "/"                    # Location of the requirements.txt file
    schedule:
      interval: "weekly"              # Check for updates weekly
    open-pull-requests-limit: 15       # Limit the number of open PRs (optional)
    
  #  ignore:                           # Specify any packages to ignore (optional)
  #    - dependency-name: "some-package"  # Replace with actual package name
  #      versions: ["<2.0.0"]          # Replace with actual version constraints
